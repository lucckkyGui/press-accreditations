# Raport statusu wdrożenia — 2026-06-08

_Read-only. Oparty na: `git log` brancha (prawda o ZROBIONYM), `docs/RELEASE-BACKLOG.md` + `docs/RELEASE-PLAN-R1.md` (oba z 2026-06-03 → częściowo nieaktualne), stanie brancha._

> ⚠️ **Rozbieżność docs ↔ git.** Oba docs są z 06-03 i NIE znają pracy z 06-08 (cały skaner, logowanie, część kodów numerycznych są w git log, nie w docs). Backlog sekcja A wciąż listuje „offline prefetch" jako TODO — w git log **zrobiony** (`9c92f54`). Poniżej git log = źródło prawdy o tym, co zrobione.

---

## 1. Gdzie jesteśmy

- **Branch:** `fix/coverage-submit-public` — **77 commitów PRZED `origin/main`, 0 za** → czysty fast-forward merge możliwy. **Nie zmergowane do main.**
- **Gate (zweryfikowany w tej sesji, ostatni przebieg):** `typecheck` **0** błędów · `lint` **0 błędów** (33 pre-existing warnings) · `test:run` **148/148**. Node 22.
- **Supabase `jozg`:** migracja kodów numerycznych **prawdopodobnie zaaplikowana** — commit `dc80d62 chore(types): regen po migracji qr_code` (typy regeneruje się z żywej bazy → implikuje `db push`). **Z repo NIE da się potwierdzić** faktycznego stanu bazy, deployu edge functions ani sekretów — to wymaga dostępu do jozg/Vercel.
- **Vercel:** z repo nie widać, który commit jest live ani czy deploy przeszedł. Nie do oceny z repo.

---

## 2. Co zrobione (z git log, tematycznie)

**Rekonsyliacja schematu kod↔baza (typecheck 103 → 0)**
- `fc18bd0` Krok A: `accreditations`/`accreditation_types` do schematu bazy; wygaszenie Media Portal + martwych serwisów.
- `9cdb8b2` Krok B: deprecate martwy frontend invitations/email + `accreditationRequestService`; `guests.invitation_*` → `email_status`; fix latentne `accreditation_requests`.
- `c8a2a75` P0-3: deprecate `AccreditationManagement` (martwy panel); zostaje `MediaVerificationPanel`.
- `39e5719` higiena `as any` 71→22 (+ fix latentny `PendingAccreditationsCard`).
- `2401872` typy z jozg; `56f6377` snapshot schematu jozg (źródło prawdy out-of-band).

**Kody gości — numeryczne + UNIQUE + migracja**
- `17567ae` numeryczny 10-cyfrowy `qr_code` + UNIQUE (trigger DB); rozdział `qr_code`(skan) ↔ `pass_token`(/pass); RPC `get_pass_by_token`; QR = numer (drop JSON); usunięty martwy `qrCodeGenerator`.
- `f868826` rename migracji `20260330` na pełny 14-cyfrowy timestamp; `dc80d62` regen `types.ts` po migracji.
- `f245c53` modal „Kod QR gościa" — realny `qr_code` przez `qrToDataURL` (zamiast statycznej ikony).

**Skaner — offline / layout / i18n / UX wyniku / kamera**
- `9c92f54` offline prefetch manifestu podpięty do `/scanner` (EVENT-CRITICAL z backlogu A) + guard one-shot + listener online.
- `fcbccf5` mobile-first layout; `59277cd` fix ścieżek i18n `scanner.*`→`pages.scanner.*` + klucze.
- `bc82673` pełnoekranowy popup wyniku skanu (kolor wg STATUS_META, trwały, pauza skanów); `4c9d3ae` styl input+overlay w tokenach designu.
- `3367707` kwadratowy podgląd + usunięcie maski qrbox; `390f8cf` fix regresji layoutu (mobile scroll / desktop kiosk); `c5237b0` fix czarnego podglądu (`#qr-reader !absolute` vs wymuszony przez html5-qrcode inline relative).

**Logowanie**
- `8c6bca7` Enter/autofill logują zamiast otwierać reset (`type="button"` na linkach + `autocomplete=username/current-password`).

**Infra / ergonomia / wcześniej**
- `0782e3d` etykieta wersji (SHA+branch+build-time); `1d8eb3a` `coverage-submit` public (`verify_jwt=false`); `e171f29` env fail-fast (K3) + jeden lockfile; `d9204f7` `project_id` → jozg + deploy docs.

---

## 3. R1 — co zostało (backlog sekcja A + plan R1)

### Czysta praca kodowa (w repo, niezależna od zewnętrznych)
- **Smoke E2E (Playwright)** głównej ścieżki: submission → approve → QR → check-in → coverage → report. Brak `playwright.config` w repo → **niezrobione.** ~1 sesja.
- **Dostęp skanera dla obsługi bramki — DECYZJA + drobna zmiana.** Dziś `/scanner` tylko `admin`/`organizer`; `staff` nie wejdzie. Decyzja: dopuścić `staff` (stopgap) vs „kod skanera" (epik F). Po decyzji zmiana kodu trywialna.
- *(Plan R1 P1 1b/1c/1d — kody numeryczne, QR skanowalny, layout mobile — **ZROBIONE** per git log; backlog ich nie odznaczył.)*

### 🔴 Blokery ZEWNĘTRZNE (nie czysty kod — wymagają działań poza repo)
- **Deploy edge functions + sekrety** — `supabase functions deploy` + `RESEND_API_KEY`, `PUBLIC_APP_URL` (realny URL, nie localhost), `ALLOWED_ORIGINS`. **Bez tego decyzja+mail i część ścieżek nie działają na prod.** Wymaga: sekretów od właściciela + dostępu do projektu. *Z repo nie da się stwierdzić, czy już zrobione.*
- **Catch-up migracje (wymagają Dockera)** — 4 tabele out-of-band (`accreditations`, `accreditation_types`, `media_registrations`, `media_documents`) bez migracji + drift RPC `process_qr_check_in` (migracja 3-param vs wdrożony 5-param). Repo nie odtworzy dziś schematu na świeżym projekcie. Wymaga instalacji Dockera + poprawnej kolejności migracji.
- **Merge do `main`** — po zielonym E2E (77 commitów, FF możliwy).

> **Wniosek:** czystej pracy kodowej R1 zostało mało (E2E + 1 decyzja+drobiazg). **Ścieżkę krytyczną do „R1 wdrożone" zdominują blokery zewnętrzne** (sekrety, Docker, deploy), nie kodowanie.

---

## 4. R2 — plany (backlog sekcja D)

- `strict: true` przyrostowo + redukcja `: any` (było 200×).
- Rekonsyliacja liczników CRM — widok/agregacja zamiast inkrementów (idempotencja).
- Trwały rate limiter (Postgres/Upstash) dla publicznych endpointów (dziś in-memory).
- Sync `accreditations.is_checked_in` z check-inem (jeśli `accreditations` zostaje po rekonsyliacji).
- Cron reminderów coverage (`pg_cron`/scheduler dla `coverage-reminders mode=cron`).
- Testy integracyjne serwisów Supabase (lokalny Supabase + vitest).

---

## 5. Tempo i szacowany czas do wdrożenia R1

**Definicja „wdrożone R1":** merge do `main` + deploy edge functions + ustawione sekrety + smoke E2E zielony + go-live.

**Założenia (wprost):**
- Kadencja sesji bursty/nieregularna: aktywne dni w oknie 06-01…06-08 to **06-01, 06-02, 06-03, 06-08** (4 z 8 dni; przerwa 06-04…06-07). Produktywność na dzień aktywny: 3 / 4 / 11 / 11 commitów → silne sesje ~10 commitów, ale ~1–2 dni aktywne / tydzień.
- Czysta praca kodowa R1 ≈ **1–2 sesje** (E2E + decyzja skanera).
- Blokery zewnętrzne wymagają działań właściciela/infra (sekrety, Docker) — czas zależy od Ciebie, nie od kodu.

**Widełki (NIE jedna data):**
- **Optymistyczny ~1 tydzień:** Docker i sekrety gotowe szybko; 1–2 skupione sesje (E2E + catch-up migracje + deploy); E2E zielony od ręki; merge.
- **Realny ~2–3 tygodnie:** bursty kadencja + koordynacja sekretów/Dockera + catch-up migracje (kolejność, walidacja) + E2E wyłapuje kilka realnych bugów check-in/coverage do poprawy.
- **Pesymistyczny ~4–6 tygodni:** catch-up migracje odsłaniają poważniejszy dług schematu; E2E ujawnia bugi w decyzji/mailu/coverage; opóźnienia po stronie sekretów/dostępów.

**Największe ryzyka dla terminu:**
1. **Deploy edge functions + sekrety** — zewnętrzne, blokuje mail/decyzję/coverage na prod; bez tego smoke E2E nie przejdzie end-to-end.
2. **Catch-up migracje + Docker** — wymaga instalacji Dockera i poprawnej kolejności; tu może wyjść więcej długu schematu niż widać.
3. **Smoke E2E** — pierwszy pełny przebieg realnej ścieżki może odsłonić bugi runtime, których gate (typecheck/lint/unit) nie łapie (jak czarny podgląd kamery).
4. **Merge 77 commitów do main** — sam FF jest niski-ryzyko, ale to duża powierzchnia bez CI na PR (sekcja C: CI/pre-commit niezrobione).

---

## Czego NIE da się ocenić z repo (szczerze)
- Faktyczny stan bazy `jozg` (czy migracja kodów i ewentualne inne realnie zaaplikowane) — implikowane przez `dc80d62`, nie potwierdzone.
- Czy edge functions wdrożone i sekrety ustawione.
- Który commit jest live na Vercel i czy deploy zielony.
- Wynik testów na realnym urządzeniu (kamera HTTPS/telefon, smoke check-in) — gate ich nie obejmuje.
